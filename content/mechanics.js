// Reusable mechanics live here instead of being hard-coded into the renderer.
//
// Example:
//
// export function lifesteal(ctx, amount) {
//   ctx.heal(amount);
// }
//
// The engine exposes a deliberately small context API.
// More mechanics can be added without rewriting the battle loop.
export {};
